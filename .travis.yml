---
language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t alexjfisher/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true alexjfisher/docker-react npm run test
